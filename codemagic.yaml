workflows:
  android-build:
    name: Android Build Workflow
    instance_type: mac_mini_m2
    max_build_duration: 60
    scripts:
      - name: Install dependencies
        script: npm install
      - name: Build Android APK
        script: npx eas build --platform android --non-interactive --local
    artifacts:
      - "*.apk"
